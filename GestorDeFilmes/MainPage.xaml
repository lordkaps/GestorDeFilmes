<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GestorDeFilmes.MainPage">
    
    
    <ScrollView>
        <VerticalStackLayout Padding="20">
            <SearchBar x:Name="SearchBarMovies" 
                   Placeholder="Buscar filmes..." 
                   SearchButtonPressed="OnSearchPressed"/>
            <CollectionView x:Name="MoviesCollectionView">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="10">
                            <StackLayout Orientation="Horizontal">
                                <Image Source="{Binding FullPosterUrl}" WidthRequest="100" HeightRequest="150"/>
                                <StackLayout Padding="10">
                                    <Label Text="{Binding Title}" FontSize="18" FontAttributes="Bold"/>
                                    <Label Text="{Binding Overview}" FontSize="14" LineBreakMode="WordWrap"/>
                                </StackLayout>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
